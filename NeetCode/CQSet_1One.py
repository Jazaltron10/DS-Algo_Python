# CONTAINS DUPLICATE
# This is the First question from the Neetcode Series 
import math 
"""
Question 
Given an integer array nums, return true if any value appears at least twice in the array, and return false if every element is distinct.

input: nums = [1,2,3,1]
output: true

input: nums = [1,2,3,4]
output: false

"""


nums = 4
numsi = -4
print (math.sqrt(nums))
print (abs(nums))


    # static int firstDuplicates_Optimized(int[] a) {
    #     // This has O(n) time and O(1) space
    #     /**
    #      * Every detail of a problem matters because they can often times help you to
    #      * get the most optimized solution
    #      */
    #     for (int i = 0; i < a.length; i++) {
    #         if (a[Math.abs(a[i]) - 1] < 0) {
    #             return Math.abs(a[i]);
    #         } else {
    #             a[Math.abs(a[i]) - 1] = -a[Math.abs(a[i]) - 1];
    #         }
    #     }
    #     return -1;
    # }

def containsduplicates():
    if










































